const intialState = {
  questions: [
    { id: '1', title: ' are you dreamer?' },
    { id: '2', title: ' are you ...?' },
  ],
}

export default (state = intialState, actions) => {
  switch (actions.type) {
    default:
      return state
  }
}
